# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
(Dieses Projekt nutzt Expo SDK 54 — siehe `expo` in package.json. Bei einem
SDK-Upgrade auch diese Referenz mit anheben.)
